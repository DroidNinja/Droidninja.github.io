{
  "scripts": {
    "server": "PATH=\"$PWD/scripts:$PATH\" hugo server",
    "build": "PATH=\"$PWD/scripts:$PATH\" hugo --gc --minify"
  },
  "devDependencies": {
    "sass-embedded": "^1.100.0"
  }
}
